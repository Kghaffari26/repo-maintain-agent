[settings]
stale_days = 14
include_drafts = false
dup_threshold = 0.45
max_triage_per_repo_per_run = 25
max_changelog_items = 80
max_writes_per_run = 15
max_writes_per_repo_per_day = 10

[health.penalties]
untriaged_7d_each = 3
untriaged_cap = 30
stale_pr_each = 5
stale_pr_cap = 25
first_response_72h = 10
first_response_168h = 15
ci_failure = 20
release_overdue = 10
community_missing_each = 3

[[repo]]
full_name = "Kghaffari26/agents-core"
role = "own"
allow_apply = false
token = "default"
label_map = { bug = "bug", feature = "enhancement", question = "question", docs = "documentation", chore = "chore" }
priority_labels = {}
extra_triaged_labels = ["triaged"]
ignore_labels = ["wontfix", "on-hold"]

[[repo]]
full_name = "Kghaffari26/real-estate-agent"
role = "own"
allow_apply = false
token = "default"
label_map = { bug = "bug", feature = "enhancement", question = "question", docs = "documentation", chore = "chore" }
priority_labels = {}
extra_triaged_labels = ["triaged"]
ignore_labels = ["wontfix", "on-hold"]

[[repo]]
full_name = "Kghaffari26/sam-agent"
role = "own"
allow_apply = false
token = "default"
label_map = { bug = "bug", feature = "enhancement", question = "question", docs = "documentation", chore = "chore" }
priority_labels = {}
extra_triaged_labels = ["triaged"]
ignore_labels = ["wontfix", "on-hold"]

[[repo]]
full_name = "Kghaffari26/macro-fed-agent"
role = "own"
allow_apply = false
token = "default"
label_map = { bug = "bug", feature = "enhancement", question = "question", docs = "documentation", chore = "chore" }
priority_labels = {}
extra_triaged_labels = ["triaged"]
ignore_labels = ["wontfix", "on-hold"]

# NOTE: tonight's instructions gave this full_name as "Kghaffari26/repo-maint-agent",
# but the repo this session is actually running in (per its GitHub access scope) is
# "Kghaffari26/repo-maintain-agent" (with "ain"). Used the exact name given verbatim
# rather than silently "fixing" it -- confirm in the morning; see STATUS.md.
[[repo]]
full_name = "Kghaffari26/repo-maint-agent"
role = "own"
allow_apply = false
token = "default"
label_map = { bug = "bug", feature = "enhancement", question = "question", docs = "documentation", chore = "chore" }
priority_labels = {}
extra_triaged_labels = ["triaged"]
ignore_labels = ["wontfix", "on-hold"]

[[repo]]
full_name = "Kghaffari26/agents-hub"
role = "own"
allow_apply = false
token = "default"
label_map = { bug = "bug", feature = "enhancement", question = "question", docs = "documentation", chore = "chore" }
priority_labels = {}
extra_triaged_labels = ["triaged"]
ignore_labels = ["wontfix", "on-hold"]

# Add once you've created it (§2, §9). This is where apply mode gets demonstrated
# safely -- real labels/comments the agent made, on a repo built for exactly that.
# [[repo]]
# full_name = "Kghaffari26/agents-hub-sandbox"
# role = "sandbox"
# allow_apply = true
# token = "repo_maint"
# label_map = { bug = "bug", feature = "enhancement", question = "question", docs = "documentation" }
# priority_labels = { p0 = "priority: critical", p1 = "priority: high", p2 = "priority: medium", p3 = "priority: low" }
