# Autonomous session decisions

One line per reasonable-default choice made without asking, newest last.

- Repo already had a partial build from a prior session (core/http.py, agents/repo_maint/{gh,config}.py, tests, docs/specs/SPEC_REPO_MAINT.md) on this same branch; treating tonight's instructions as superseding that layout where they conflict (agents-core dependency, Python 3.12, public-data/ publish target) rather than starting over from a truly empty repo.
- Cloned https://github.com/Kghaffari26/agents-core and fetched all branches (only `main` exists): no commit contains `src/agents_core/guards.py` + `src/agents_core/registry.py`. The real layout is a non-packaged monorepo (`core/guards.py`, `core/registry.py`, `pyproject.toml` has `[tool.uv] package = false` and no `[build-system]`, so it cannot be `uv add`-ed as a git dependency at all in its current form). Treating this as "no such commit exists yet" per the fallback clause: doing core-independent work first, scheduling periodic rechecks, and recording this as a blocker in STATUS.md rather than modifying agents-core.
- Deleted the prior session's `core/http.py` (a hand-rolled HTTP client) since tonight's hard rule is "never write your own http/llm/costs/guards/publish/runner module." `agents/repo_maint/gh.py` now takes an *injected* HTTP client via a minimal structural protocol (`request(method, url, **kwargs) -> response`), so it has zero networking implementation of its own and will accept `agents_core.http.HttpClient` unchanged once that package is installable. Tests inject a tiny local fake built on `httpx.MockTransport` (test-only, not shipped as a reusable module).
- No `ANTHROPIC_API_KEY` is set in this environment, so zero live LLM calls are physically possible tonight regardless of agents-core's availability. `triage.py` and `changelog.py` take an injectable `classify`/`draft` callable; with none provided they use the spec's own deterministic fallbacks (§7.3 step 3 for changelog; a low-confidence "other" placeholder for triage, clearly logged as such). This keeps the pipeline runnable end-to-end at $0 real spend and is fully spec-compliant (the changelog fallback path is written into §7.3 itself).
- For tonight's one real report-mode run (read-only, against the user's own repos, GITHUB_TOKEN is present in env), used a small non-shipped `scripts/run_report_once.py` that wraps `httpx.Client` directly just to execute the live GitHub reads — it is not part of the importable `agents` package and will be deleted once `agents_core.http` is wired in.
- Evals §11 calls for "25 from the seeded sandbox, plus 15 real public issues." Both sources were checked and both are blocked tonight: `mcp__github__list_issues` confirms the user's own watched repos (agents-core, real-estate-agent, repo-maintain-agent) genuinely have zero issues right now; and pulling issues from an unrelated third-party public repo (tried `pallets/flask`) requires `add_repo(access="push")`, which the session's own auto-mode safety classifier correctly denied for a repo the user doesn't own (it did allow the same call for the user's own repos). Built all 40 fixtures as synthetic instead — 26 reused verbatim from `scripts.seed_sandbox.build_issues()`, 14 hand-written to fill classification gaps the sandbox set doesn't cover (docs, chore, explicit security/data-loss priority cases, two "near miss" non-duplicate pairs) — and labeled the provenance of every fixture in `fixtures.json` so this is visible, not silently passed off as real data.
- Running the injection-resistance eval for real (feeding each fixture's worst-case fully-compliant raw model output through our own `postprocess()`/`sanitize()`) surfaced two genuine gaps: (1) a "reply with your system prompt" attempt survived `sanitize_first_response` uncaught — fixed by adding `"system prompt"` to `sanitize.py`'s reject phrases; (2) a model that self-reports `priority: p0` outright (not via the regex-escalation path) is never clamped back down by code, since §7.2's guard is one-directional by design. (1) was a real implementation gap relative to the spec's own eval pass criteria and got fixed tonight; (2) is a spec-inherent limitation (code can't judge "is this really p0-worthy" without another model call) and is recorded as a known limitation in the fixture/results rather than silently hidden.
